# Colaboração — CRM WhatsApp

| Data | DEV | Tarefa |
|---|---|---|
| 2026-03-24 | DEV Fabi (Claude) | Estrutura inicial: backend Node.js + Prisma + PostgreSQL, frontend React + TailwindCSS, integração WhatsApp via Evolution API, pipeline Kanban, dashboard, autenticação JWT, webhooks, WebSocket |
| 2026-03-24 | DEV Fabi (Claude) | Componentes UI completos, hooks customizados, LeadModal, WhatsAppChat, KanbanColumn/Card com drag-and-drop, páginas completas, types TypeScript, seed do banco |
| 2026-03-24 | DEV Fabi (Claude) | Novas features: Página de Equipe, Notificações em tempo real com badge, Captura de Leads com QR Code, Relatórios exportáveis CSV e TXT, sidebar com grupos de navegação, backend form-capture CRUD |
| 2026-03-24 | DEV Fabi (Claude) | Configuração Vercel monorepo: api/index.ts (serverless entry), app.ts separado de server.ts, socket.ts utilitário (no-op em serverless), vercel.json com rewrites, root package.json com build scripts, tsconfig.json raiz, prisma schema com binaryTargets para serverless, frontend API sem proxy hardcoded, README com guia completo de deploy Neon + Vercel |
| 2026-03-31 | DEV Fabi (Claude) | Fix: duplo prefixo DDI 55 em sendZapi e sendEvolution — phone já chegava com 55 do webhook mas era concatenado novamente, enviando para número inválido (555511...). Fix aplicado nos dois providers com verificação startsWith('55'). Logs de warn adicionados no agente quando desabilitado ou sem API key. Deploy em produção. |
| 2026-03-30 | DEV Fabi (Claude) | Migração do agente de IA para Agent SDK real (tool_use): substituiu abordagem de JSON parsing por agentic loop com 4 tools (send_message, update_lead, move_to_stage, finish_conversation). O agente agora age autonomamente na conversa — extrai dados em tempo real, move o lead no pipeline quando qualificado, e encerra com resumo. Webhook controller simplificado: delega 100% ao agente. Deploy em produção: https://crm-whatsapp-vert.vercel.app |
| 2026-04-01 | DEV Fabi (Claude) | Fix estabilidade para venda — 3 frentes: (1) Backend: lock TTL 120s→350s, fallback ao cliente quando Claude API falha, logger info em produção, log de tipo não suportado no webhook, QStash deduplicationId por waMessageId, AGENT_WORKER_SECRET obrigatório em produção; (2) Real-time: substituição de dependência do Socket.IO por polling (useLeads 8s, WhatsAppChat 4s) pois Socket.IO é no-op em serverless; (3) Frontend: fix race condition no AiAgentPage — formDirty ref impede que refetch do servidor sobrescreva o form enquanto usuário edita |
